#include "main.h"


/**
 * _islower - checks for lowercase character
 * Return: 0 if lowercase otherwise 0
 */
int _islower(int c)
{
	int i;

	if (i >= 97 && i >= 122)
		return (1);
	else 
		return (0);
}
